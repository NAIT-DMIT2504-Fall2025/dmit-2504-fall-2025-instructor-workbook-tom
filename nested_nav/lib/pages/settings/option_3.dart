import 'package:flutter/material.dart';

class Option3 extends StatelessWidget {
  const Option3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is where I would put Option 3 stuff... If I had any'),
      ),
    );
  }
}
