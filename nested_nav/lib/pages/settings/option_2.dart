import 'package:flutter/material.dart';

class Option2 extends StatelessWidget {
  const Option2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is where I would put Option 2 stuff... If I had any'),
      ),
    );
  }
}
